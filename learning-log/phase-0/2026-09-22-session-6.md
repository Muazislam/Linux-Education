# Learning Session: Phase 0 - Documentation-First Investigation with `wc` (Task D4 Completion)

## Date
2026-09-22 (Night Session, ~22:31)

## Module
Phase 0 - Environment, Safety, and Baseline Setup

## Topic
Completion of Baseline Diagnostic Task D4: Documentation-First Workflow using `wc` (word, line, and byte counting; flag analysis; edge cases; and option parameter constraints)

## Objective
Demonstrate complete independence ($H0$ hint level) in extracting command functionality, syntax, flags, and edge cases directly from local system documentation (`man wc` and `wc --help`) without relying on web searches.

## What I Practiced & Explored
1. **Core Command Understanding**:
   - Analyzed `wc` (word count): prints newline, word, and byte counts for files or standard input.
   - Identified its role in Unix pipelines (chaining commands to count output lines, e.g. `ps aux | wc -l`).
2. **Flag Exploration & Hands-on Verification**:
   - Tested multiple options on `'Linux Fundamentals.pdf'`:
     - `-l` / `--lines`: printed newline count (`35661`).
     - `-m` / `--chars`: printed character count (`1629574`).
     - `-c` / `--bytes`: printed raw byte count (`2744617`).
     - `-w` / `--words`: printed whitespace-delimited word count (`64042`).
     - `-L` / `--max-line-length`: printed maximum display width (`1511`).
     - `--debug`: checked line count acceleration indicators.
3. **Caveat, Risk & Edge Case Analysis**:
   - **The Stdin Trap**: Noticed the documentation warning: *"With no FILE, or when FILE is -, read standard input."* Identified this as a common edge case where running `wc` without arguments causes the shell to appear "frozen" while waiting for user keystrokes until EOF (`Ctrl+D`).
   - **Bytes vs Characters**: Observed that byte count (`-c` = 2,744,617) significantly exceeded character count (`-m` = 1,629,574), illustrating how modern multi-byte character encodings (UTF-8) and binary files represent data.
   - **Option Constraints**: Tested `--files0-from=F` and analyzed the parser error: `extra operand ... file operands cannot be combined with --files0-from`.

## Evidence
- Recorded in `assessments/phase-0/baseline-diagnostic.md` (Task D4 marked completed with $H0$ rating).
- Multi-flag execution traces on `'Linux Fundamentals.pdf'`.

## Next Action
Proceed to Phase 0 Baseline Diagnostic Task D5: Process Inspection & Distinctions.
